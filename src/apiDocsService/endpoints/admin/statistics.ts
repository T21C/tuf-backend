import { EndpointDefinition } from '../../services/DocumentationService.js';

// Note: Statistics endpoints would be documented here when they are implemented
// Currently, the statistics route exists but doesn't expose public endpoints

const statisticsEndpoints: EndpointDefinition[] = [
  // TODO: Add statistics endpoints when available
];

export default statisticsEndpoints;
