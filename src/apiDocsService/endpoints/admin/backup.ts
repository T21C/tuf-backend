import { EndpointDefinition } from '../../services/DocumentationService.js';

// Note: Backup endpoints would be documented here when they are implemented
// Currently, the backup route exists but doesn't expose public endpoints

const backupEndpoints: EndpointDefinition[] = [
  // TODO: Add backup endpoints when available
];

export default backupEndpoints;
