import { EndpointDefinition } from '../../services/DocumentationService.js';

// Note: Audit log endpoints would be documented here when they are implemented
// Currently, the audit log route exists but doesn't expose public endpoints

const auditLogEndpoints: EndpointDefinition[] = [
  // TODO: Add audit log endpoints when available
];

export default auditLogEndpoints;
