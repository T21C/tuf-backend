import { EndpointDefinition } from '../../services/DocumentationService.js';

const channelParserEndpoints: EndpointDefinition[] = [
  // Note: channelParser.ts contains utility functions for parsing announcement channels
  // These are not API endpoints but internal functions used by webhook endpoints
];

export default channelParserEndpoints;
